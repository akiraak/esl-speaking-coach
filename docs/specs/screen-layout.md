# 画面レイアウト・仕様

2026-07-25 決定（経緯は `docs/plans/archive/screen-layout-spec.md`）。実装は TODO「会話画面の UI」ほかで行う。

## 決定サマリ

| 項目 | 決定 |
| --- | --- |
| トップ画面 | 常設のグループトークルーム（起動で即チャット画面。トーク一覧・タブは作らない） |
| トピック | チャット内に投稿されるトピックカードから選択、または自分でトピックを作成して開始 |
| 入力切替 | LINE 風入力バー。マイクボタンで音声モードへ変形、キーボードボタンでテキストモードへ戻る |
| AI 2 キャラのターン進行 | **台本方式で決定**（2026-07-25。詳細は [conversation-design.md](conversation-design.md)） |
| ビジュアルデザイン | **案 D「ポップ・スタディ」**（2026-07-25 決定。下記「ビジュアルデザイン」参照） |

## 画面一覧

| 画面 | 役割 |
| --- | --- |
| トーク画面 | 起動直後に表示される主画面。AI 2 キャラとのグループチャット |
| 管理画面 | 会話ログ（発話 + 調査用ログ）・利用料金・記憶の閲覧（トーク画面のヘッダから開く） |

設定画面は持たない。API キーは `.secrets/<provider>-api-key` を置いて `./run-install-iphone.sh` /
`./run-simulator.sh` から Keychain へシードする運用（CLAUDE.md「セキュリティ」参照）。

過去セッションの振り返りは、当面はトーク画面のタイムラインを遡ることで行う（フィードバック一覧などの専用画面は必要になったら別タスクで検討）。

## トーク画面

```
┌────────────────────────────┐
│ ESL Group (3)            📊 │ ← ヘッダ: ルーム名 + メンバー数 + 管理画面ボタン
├────────────────────────────┤
│ ── 7/25 Travel plans ──    │ ← セッション区切り
│ (Mia) [Mia]                │
│  Where would you go?       │ ← AI: 左寄せ・アバター + 名前
│              I'd go to Peru│ ← ユーザー: 右寄せ・アクセント色
│ ┌────────────────────────┐ │
│ │ 📝 Session Feedback     │ │ ← フィードバックカード
│ └────────────────────────┘ │
│ ┌────────────────────────┐ │
│ │ 📌 次のトピック          │ │ ← トピックカード
│ │ ▷ 旅行の計画            │ │
│ │ ▷ やめられない食べ物    │ │
│ │ ▷ フリートーク          │ │
│ │ [🔄 他の候補] [＋自分で] │ │
│ └────────────────────────┘ │
│ [🏁 このトピックを終了     ] │ ← 終了ボタン（下端に固定・セッション中のみ）
├────────────────────────────┤
│ [🎤] [Aa メッセージ…] [↑]  │ ← 入力バー（テキストモード時）
└────────────────────────────┘
```

### ヘッダ

- ルーム名（固定。例: "ESL Group"）+ メンバー数
- 右端に管理画面ボタン（📊 `chart.bar.doc.horizontal`）→ 管理画面をシート表示
- セッション終了ボタンはヘッダには置かない（下記「タイムライン」参照）

### タイムライン

表示要素は次の 5 種類。すべて同一のスクロールタイムラインに時系列で積む。

1. **AI メッセージ**: 左寄せ。アバター + 名前ラベル付き（LINE のグループトーク準拠）。2 キャラはそれぞれ固有のアバター・名前を持つ
2. **ユーザーメッセージ**: 右寄せ・アクセントカラーの吹き出し。自分のアバター・名前は表示しない（LINE 準拠）
3. **トピックカード**: 後述
4. **フィードバックカード**: セッション終了時に投稿される（内容・レイアウトは [session-feedback.md](session-feedback.md)）
5. **システムメッセージ**: エラー・セッション区切り（日付 + トピック名）。中央寄せのグレー小文字テキスト
   - レイテンシ計測（`体感 …ms | TTFT …`）と技術通知（`STT へ接続中…` / `STT 再接続完了` /
     `stop_reason: …` など）はトーク画面には出さず、管理画面の会話ログにのみ残す（調査用）

表示挙動:

- 新規メッセージ追加時は最下部へ自動スクロール（ユーザーが手動で遡っている間は自動スクロールしない）
- **ライブ文字起こし**: 発話中の partial transcript は右寄せ・半透明の吹き出しで表示し、確定後に通常のユーザーメッセージへ置き換える
- **応答生成中**: LINE 風の「〜が入力中…」インジケータをタイムライン最下部に表示
- **読み上げ中**: 発話中キャラのアバター横にスピーカーアイコン（🔊）を表示
- 検証用の RMS 数値・レイテンシ表示・エンジン切替 UI はトーク画面には出さない（レイテンシと技術通知は管理画面の会話ログで見る）
- **セッション終了ボタン（🏁 このトピックを終了）**: タイムライン下端（入力バーのすぐ上）に横長・アクセント色で**固定表示**する（2026-07-26 変更。⋯ メニュー内 → ヘッダ常設 → 入力バー内 → 現在の形。入力バーを縦に伸ばさず、会話が追記されても位置が動かないようにするため）
  - スクロールには乗せない（`overlay(alignment: .bottom)`）。最下部アンカーにボタン高さぶんの余白を持たせ、最新メッセージがボタンに隠れないようにする
  - セッション中（`isSessionActive`）のみ表示。エラー再開バー表示中は出さない
  - 誤タップで取り消せないフィードバック生成が走るため、アラートで確認を挟む

### トピックカード

- **投稿タイミング**: 初回起動時、およびセッション終了（フィードバックカード投稿）直後に自動で投稿する
- **内容**: Claude が事前生成したトピック候補 3 件 + 「フリートーク」。各候補は日本語タイトル + フック 1 文（2026-07-25 追加、2026-07-26 に英語→日本語へ変更。生成仕様は [conversation-design.md](conversation-design.md)）。候補ピルにはタイトルを表示し、フック文を小さく添える。会話へは `[New topic: <日本語タイトル>]` のまま渡し、会話自体は英語で行われる
- **操作**:
  - 候補をタップ → そのトピックでセッション開始
  - 「🔄 他の候補」→ 候補 3 件を再生成して同カードを差し替え
  - 「＋ 自分で作る」→ 1 行のテキスト入力（シートまたはアラート）を開き、入力したトピックでセッション開始
- 選択済み・過去のカードはグレーアウトして履歴に残す（タップ無効）

### 入力バー

LINE 同様の入力バーを基本形とし、マイクボタンで音声モードへ「変形」する。

```
テキストモード:
│ [🎤] [Aa メッセージを入力…] [↑] │
        ↓ 🎤 タップ / ⌨ タップで復帰
音声モード（ハンズフリー連続）:
│ [⌨] ● 〰〰〰〰〰〰〰〰 [⏸]     │
```

- **テキストモード**: テキストフィールド + 送信ボタン。左端のマイクボタンで音声モードへ
- **音声モード**: ハンズフリー連続。サーバ VAD（無音 800ms）でターン検出、読み上げ中の barge-in 可。聞き取り中は波形（マイクレベル連動）をアニメーション表示。⏸ で聞き取り一時停止 / 再開、左端のキーボードボタンでテキストモードへ
- モードはセッション・アプリ再起動をまたいで保持する
- どちらのモードでも会話履歴・AI 応答・読み上げの挙動は同一（テキスト送信でも AI は音声で返す）

### 状態とバーの対応

| 会話状態 | 入力バー（音声モード） | タイムライン |
| --- | --- | --- |
| 聞き取り中 | 波形アニメーション | （発話中は半透明のライブ吹き出し） |
| 応答待ち | 波形停止・薄色 | 「〜が入力中…」 |
| 読み上げ中 | 波形停止（barge-in は受付） | 発話キャラに 🔊 |
| エラー / 切断 | 再接続ボタン表示 | システムメッセージ |

## セッションの流れ

1. トピックカードで候補を選ぶ or 自作トピックを入力 → セッション開始（区切りのシステムメッセージを投稿）
2. 音声またはテキストで会話する
3. タイムライン下端に固定表示される「このトピックを終了」ボタン（確認アラートで承認）、または学習者の goodbye（AI が制御行 `[end]` を出力しアプリが検知）→ フィードバック生成（別タスク）→ フィードバックカード投稿
4. 直後に次のトピックカードが自動投稿される（→ 1 に戻る）

- アプリを閉じた場合、進行中セッションは次回起動時にそのまま継続表示する

## キャラクター

- AI 2 キャラは名前・アバター・人格・TTS voice をアプリ内定義の固定値で持つ（アプリ内での編集は将来検討）
- アバターは当面イニシャル or SF Symbols ベースのプレースホルダで良い（画像アセットは任意）
- 名前・人格・voice は決定済み（2026-07-25）: **Chobi**（先生・Leda・ピンク）/ **Naruko**（仲間の生徒・Aoede・薄い緑）。詳細は [conversation-design.md](conversation-design.md)

## ビジュアルデザイン（案 D「ポップ・スタディ」）

4 案比較（LINE クラシック / iOS ミニマル / ダークフォーカス / ポップ・スタディ）から選定。モックアップは `docs/specs/screen-design-mockups.html`（案 D を採用）。

**トーン**: 語学アプリ寄りの明るい暖色。大きめの角丸とカラフルなアバターで 2 キャラの「相手感」を最大化し、続けたくなる楽しさを重視する。

### カラーパレット（ライト）

| 要素 | 値 |
| --- | --- |
| チャット背景 | `#FDF3E3`（暖色クリーム） |
| ヘッダ / 入力バー背景 | `#FFFFFF` |
| AI 吹き出し | 背景 `#FFFFFF` / 文字 `#45351F` |
| ユーザー吹き出し | 背景 `#F0642F`（コーラルオレンジ） / 文字 `#FFF6EF` |
| ライブ文字起こし吹き出し | `#F0642F` の 20% 透過 / 文字 `#B5502A` / 斜体 |
| アクセント（波形・送信ボタン・カードタイトル・🔊 表示） | `#F0642F` |
| トピック / フィードバックカード | 白地、枠 `rgba(120,80,40,0.12)` |
| トピック候補ピル | 通常 `#FBEEDD` / 選択済み `#FFE1D2` + 文字 `#CF4A17` |
| システムメッセージ | 文字 `#96775C` / 背景 `rgba(120,80,40,0.10)` のピル |
| 名前ラベル | `#A08464` |
| アバター | Chobi `#EF5DA8`（ピンク）/ Naruko 薄い緑（仮 `#6FCF97`、実装時に調整可）。白フチ 2px + 薄い影。モックの Mia / Leo は Chobi / Naruko に読み替える |

### 形状・タイポグラフィ

- 角丸: 吹き出し 20pt（しっぽ側の角のみ 6pt）、カード 20pt、トピック候補はピル型（full）
- 吹き出し・カードに `rgba(120,80,40,0.1)` 程度のごく薄い影
- フォントはシステムフォント。ヘッダタイトルは太め（heavy / bold）、本文 medium
- 入力バー: テキストフィールドは `#F7ECDC` のピル型、送信ボタンはアクセント色の円形

### ダークモード

暖色トーンを保ったままの再設計が必要（単純反転はしない）。配色は実装フェーズで決める（未決事項）。

## 受け入れ条件（実装タスクの確認項目)

2026-07-25 実装完了（タスク「会話画面の UI」）。シミュレータ E2E + 実機確認で全項目を確認済み。

- [x] 起動すると（キー設定済みなら）直接トーク画面が開く
- [x] AI 2 キャラのメッセージがアバター + 名前付きで左寄せ、ユーザーが右寄せで表示される
- [x] トピックカードから候補選択・再生成・自作入力の 3 操作ができ、選択でセッションが始まる
- [x] マイクボタン⇔キーボードボタンで音声 / テキストモードが往復でき、モードが再起動後も保持される
- [x] 音声モードで、聞き取り中の波形・partial transcript・「入力中…」・読み上げ中 🔊 が状態どおり表示される
- [x] テキスト送信でも音声と同じ会話フロー（AI 応答 + 読み上げ）になる
- [x] 「トピックを終える」または goodbye 発話（`[end]` 検知）でセッションが終了し、区切り表示と次のトピックカードが投稿される（2026-07-25 フィードバックカードの投稿も実装・確認済み）

## 未決事項（実装フェーズで決める）

- ダークモードの配色（案 D の暖色トーンを保った再設計。単純反転はしない）

解消済み（2026-07-25）: AI 2 キャラのターン進行方式（**台本方式**）とキャラの具体値（Chobi / Naruko。[conversation-design.md](conversation-design.md)）、フィードバックカードの内容・レイアウト（[session-feedback.md](session-feedback.md)）
